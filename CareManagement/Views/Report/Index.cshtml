@model CareManagement.Models.SCHDL.Report

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

@*<p>
    <a asp-action="Create">Create New</a>
</p>*@

@{
    ViewData["Title"] = "Create";
}

@*<h1>Create</h1>*@

<h4>Report</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="StartTime" class="control-label"></label>
                <input asp-for="StartTime" class="form-control" />
                <span asp-validation-for="StartTime" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="EndTime" class="control-label"></label>
                <input asp-for="EndTime" class="form-control" />
                <span asp-validation-for="EndTime" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="RenterId" class="control-label"></label>
                <select asp-for="RenterId" class="form-control" asp-items="ViewBag.RenterId"></select>
            </div>
            <div class="form-group">
                <label asp-for="ServiceId" class="control-label"></label>
                <select asp-for="ServiceId" class="form-control" asp-items="ViewBag.ServiceId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(modelItem => modelItem.StartTime)
            </th>
            <th>
                @Html.DisplayNameFor(modelItem => modelItem.EndTime)
            </th>
            <th>
                @Html.DisplayNameFor(modelItem => modelItem.Renter)
            </th>
            <th>
                @Html.DisplayNameFor(modelItem => modelItem.Service)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>
                @Html.DisplayFor(modelItem => modelItem.StartTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => modelItem.EndTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => modelItem.RenterId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => modelItem.ServiceId)
            </td>
            <td>
                @*<a asp-action="Edit" asp-route-id="@Model.ReportId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@Model.ReportId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@Model.ReportId">Delete</a>*@
            </td>
        </tr>
    </tbody>
</table>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
